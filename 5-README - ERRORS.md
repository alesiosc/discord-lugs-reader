# --- Application Settings ---

# Show command window with debug output (true=show cmd window, false=hide cmd window)                               │
SHOW_CMD_WINDOW=false

# Headless mode for browser automation
HEADLESS_MODE=true

# DUPLICATES=true will send every message, even if it's a duplicate.
# DUPLICATES=false will only send new, unique messages.
DUPLICATES=fa;se

# Comma-separated list of tickers to process (e.g., NQ,ES,YM,GC,RTY,CL)
ENABLED_TICKERS="NQ,ES,YM"
